class sueldo:
	def __init__(self):
		pass
	def ausu(self):
		sueldo=float(input("Ingrese  su sueldo: "))
		if sueldo <600:
			print("Ud recibira un aumento en sueldo  por lo tanto ganara:{} $".format((sueldo*0.10)+sueldo))
		else:
			print("Su sueldo es: ",sueldo)
ob=sueldo()
ob.ausu()
